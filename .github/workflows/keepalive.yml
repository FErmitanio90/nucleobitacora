name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # cada 5 minutos
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          echo "✨ Ping al backend de Render..."
          curl -I https://nucleobitacora.onrender.com/health || true
